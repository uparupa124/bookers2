<%# ここからエラーメッセージ %>
  <% if @book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book.errors.count, "error") %> prohibited this Book from being saved:</h2>

        <ul>
            <% @book.errors.full_messages.each do |message| %>
             <li><%= message %></li>
          <% end %>
        </ul>
    </div>
  <% end %>
  <%# ここまでエラーメッセージ %>
<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
      
       <%= render "books/list", book: @book, user: @user %>
    
    </div>
    
      <div class="col-md-8 offset-md-1">
        <h1>Books</h1>
          <table class="table">
            <thead>
              <tr>
                <th></th>
                  <th class="text-center"><h4>Title</h4></th>
                    <th><h4 class="text-center">Opinion</h4></th>
              </tr>
            </thead>
            
              <tbody>
           
                <% @books.each do |book| %>
                  <tr class="border-top ">
                    <td>
                      <%= link_to user_path(book.user.id) do %>
                        <%= attachment_image_tag book.user, :profile_image, :fill, 50, 50, fallback: "no_image.jpg", size:"50x50" %>
                      <% end %>
                    </td>
                      <td class="text-center"><%= link_to book.title, book_path(book.id) %></td>
                        <td class="text-center"><%= book.body %></th>
                    </tr>
                <% end %>
          
              </tbody>
          </table>
      </div>
  </div>
</div>